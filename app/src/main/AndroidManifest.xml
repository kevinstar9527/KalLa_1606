<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a05.qianfeng.edu.cn.kalla_1606">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".other.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--引导页-->
        <activity android:name=".other.ui.GuideActivity"/>

        <!--广告页-->
        <activity android:name=".other.ui.BannerActivity"></activity>

        <!--主页面-->
        <activity android:name=".other.ui.HomeActivity"></activity>

    </application>

</manifest>